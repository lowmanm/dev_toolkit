# Design Conventions

> Placeholder — same as `architecture.md`: fill this in as the codebase develops, or re-run the `principles` agent once there's enough code to derive real conventions from.

## Code-level conventions specific to this repo

TODO: patterns this repo prefers — e.g. specific module boundaries, error-handling conventions, state-management approach — beyond the org-wide `dev-scaffolding` skill's generic guidance.

## Keeping this current

Update this file in the same change that introduces or changes a design convention, not as a separate task. See the `principles-maintenance` skill.
