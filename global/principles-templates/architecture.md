# Architecture

> Placeholder — this repo didn't have enough existing code yet for the `principles` agent to derive real architecture documentation from. Fill this in as the system takes shape, or re-run the `principles` agent once there's enough code to read.

## System overview

TODO: what does this system do, at a high level? What are its major components/services, and how do they talk to each other?

## Key architectural decisions

TODO: notable decisions and why (e.g. why this datastore, why this framework, why this service boundary).

## Keeping this current

Update this file whenever a change affects the system's architecture — as part of that same change, not as separate cleanup. See the `principles-maintenance` skill.
